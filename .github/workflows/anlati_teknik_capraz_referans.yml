# ANLATI-TEKNIK CAPRAZ REFERANS (06.08.2026) - Faz V0
# Konum: .github/workflows/anlati_teknik_capraz_referans.yml
# TEK SEFERLIK, elle tetiklenir - SALT OLCUM, Pine'a hic dokunmaz.
name: Anlati Teknik Capraz Referans
on:
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  analiz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install yfinance==0.2.66
      - name: Capraz referans analizi calistir
        run: python anlati_teknik_capraz_referans.py
      - name: Sonuclari commit'le
        run: |
          git config user.name "capraz-referans-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/denetim/anlati_teknik_capraz_referans.json
          git diff --staged --quiet && exit 0
          git commit -m "Anlati-teknik capraz referans $(date -u +%Y-%m-%d)"
          for i in 1 2 3 4 5; do
            if git push; then
              echo "push basarili (deneme $i)"
              exit 0
            fi
            echo "push reddedildi, rebase + tekrar deneniyor ($i/5)"
            sleep $(( (RANDOM % 8) + 3 ))
            git pull --rebase origin main
          done
          echo "5 denemeden sonra push basarisiz"
          exit 1
